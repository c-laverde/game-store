# game-store

## Ejecutar los proyectos

1. Crear la base de datos con nombre `bd_gamestore`

2. Ejecuar el proyecto api, para subir los servicios y crear las tablas.

3. Ejecutar el proyecto web-app con el comando "npm start", ya que se agregó una configuración proxy para omitir las restricciones del CORS.
